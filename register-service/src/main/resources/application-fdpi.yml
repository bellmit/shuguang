server:
  port: 8880
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://47.94.214.55:${server.port}/eureka/

spring:
  application:
    name: register-service